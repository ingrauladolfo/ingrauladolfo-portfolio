import { API_URL } from "./config"

const projects= '/projects'
const stack= '/stack'
const experience= '/experience'
export const PROJECTS = `${API_URL}${projects}`
export const STACK = `${API_URL}${stack}`
export const EXPERIENCE = `${API_URL}${experience}`