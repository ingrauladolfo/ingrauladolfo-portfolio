import { BrowserRouter } from 'react-router-dom';
import { AppRouter } from './AppRouter';

const AppRoutes = () => (
  <BrowserRouter>
       <AppRouter />
  </BrowserRouter>
);

export default AppRoutes;
